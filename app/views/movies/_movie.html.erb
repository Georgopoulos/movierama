<li id="movie-<%= movie.id %>">
	<div class="col-md-12">
    <div>
      <span class="title"><%= movie.title %></span>
      <% if current_user?(movie.user) %>
        <small>
          <%= link_to "Edit", edit_movie_path(movie) %> |
          <%= link_to "Delete", movie,  method: :delete,
                                        data: { confirm: "Are you sure?" } %>
        </small>
      <% end %>
    </div>
  	<div class="user-timestamp">
  		Posted by 
      <%= link_to current_user?(movie.user) ? "You" : movie.user.fullname, movie.user %> 
      <span><%= time_ago_in_words(movie.created_at) %> ago.</span>
  	</div>
  	<br>
  	<div class="description"><%= movie.description %></div>
  	<br>
    <div class="row">
      <div class="col-md-3">
        <% if logged_in? && !own_movie?(movie) %>
          <%= render partial: 'votes/votes', locals: { movie: movie } if logged_in? %> 
        <% else %>
          <%= "#{movie.likes} likes" %> | <%= "#{movie.hates} hates" %>
        <% end %>
      </div>
      <% if logged_in? %>
        <div class="col-md-4 col-md-offset-5 text-right">
          <% unless own_movie?(movie) %>
            <% if v = voted?(current_user, movie) %>
              <%= render partial: 'votes/vote_msg', locals: { movie: movie, v: v } %>
            <% end %>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
  <div class="clearfix"></div>
</li>